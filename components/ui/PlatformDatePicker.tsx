import React from 'react';

export interface PlatformDatePickerProps {
  value: Date;
  onChange: (date: Date) => void;
  theme: any;
}

export const PlatformDatePicker: React.FC<PlatformDatePickerProps> = (props) => {
  return <></>; 
};
